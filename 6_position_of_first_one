int lowerbount(int *arr, int n){
	
}
int getFirstPosition(int *arr, int n)
{
	// Write your code here
	int s=0, e=n-1;
	int ans=-1;
	while(s<=e){
		int mid=s+(e-s)/2;
		if(arr[mid]>=1){ 
			ans=mid;
			e=mid-1;
		}
		else s=mid+1;
		
	}
	if(ans!=-1) return ans+1;
	return ans;

}
